<p id="notice"><%= notice %></p>

<p>
  <strong><%= t('activerecord.attributes.book.title') %>:</strong>
  <%= @book.title %>
</p>

<p>
  <strong><%= t(Book.human_attribute_name(:memo)) %>:</strong>
  <%= @book.memo %>
</p>

<p>
  <strong><%= t(Book.human_attribute_name(:author)) %>:</strong>
  <%= @book.author %>
</p>

<p>
  <strong><%= t(Book.human_attribute_name(:picture)) %>:</strong>
  <%= image_tag(@book.picture_url) if @book.picture.present? %>
</p>

<%= link_to t(Book.human_attribute_name(:edit)), edit_book_path(@book) %> |
<%= link_to t(Book.human_attribute_name(:back)), books_path %>
